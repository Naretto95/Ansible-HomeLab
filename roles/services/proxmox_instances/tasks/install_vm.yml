---

- block:

    - name: Ensure SSH keypair exists
      include_role:
        name: common/ssh_keypair

    - name: Create VM on Proxmox
      community.proxmox.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: "{{ ansible_user }}@pam"
        api_password: "{{ ansible_password }}"
        clone: "{{ vm.template }}"
        newid: "{{ vm.id }}"
        name: "{{ vm.name }}"
        node: "{{ proxmox_node }}"
        full: true
        state: present

    - name: Configure VM on Proxmox
      community.proxmox.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: "{{ ansible_user }}@pam"
        api_password: "{{ ansible_password }}"
        vmid: "{{ vm.id }}"
        node: "{{ proxmox_node }}"
        ciuser: "{{ vm.cloud_user }}"
        cipassword: "{{ vm.cloud_password }}"
        sshkeys: "{{ ssh_public_key }}"
        cores: "{{ vm.cores }}"
        scsi: "{{ vm.scsi }}"
        memory: "{{ vm.memory }}"
        net:
          net0: "virtio,bridge={{ vm.net_bridge }}"
        ipconfig:
          ipconfig0: "ip={{ vm.net_ip_address }}/{{ vm.net_subnet }},gw={{ vm.net_gateway }}"
        update: true
        update_unsafe: true

    - name: Start VM
      community.proxmox.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: "{{ ansible_user }}@pam"
        api_password: "{{ ansible_password }}"
        vmid: "{{ vm.id }}"
        node: "{{ proxmox_node }}"
        state: started

    - include_tasks: wait_ssh_healthy.yml
      vars:
        ip_address: "{{ vm.net_ip_address }}"

  delegate_to: localhost

- block:

    - name: Enable SSH password authentication
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?PasswordAuthentication\s+'
        line: 'PasswordAuthentication yes'
        backup: true
    
    - name: Ensure UsePAM is enabled
      ansible.builtin.lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?UsePAM\s+'
        line: 'UsePAM yes'
  
  delegate_to: "{{ vm.net_ip_address }}"
  vars:
    ansible_user: "{{ vm.cloud_user }}"
    ansible_password: "{{ vm.cloud_password }}"
    ansible_become: true
    ansible_become_password: "{{ vm.cloud_password }}"

- block:

    - name: Make sure SSH Key is removed from VM
      community.proxmox.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: "{{ ansible_user }}@pam"
        api_password: "{{ ansible_password }}"
        vmid: "{{ vm.id }}"
        node: "{{ proxmox_node }}"
        delete: "sshkeys"

    - name: Reboot VM via Proxmox
      community.proxmox.proxmox_kvm:
        api_host: "{{ ansible_host }}"
        api_user: "{{ ansible_user }}@pam"
        api_password: "{{ ansible_password }}"
        vmid: "{{ vm.id }}"
        node: "{{ proxmox_node }}"
        state: restarted

    - include_tasks: wait_ssh_healthy.yml
      vars:
        ip_address: "{{ vm.net_ip_address }}"

  delegate_to: localhost